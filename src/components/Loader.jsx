import '../css/loader.css'; // Asegúrate de crear este archivo CSS

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;